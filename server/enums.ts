export enum OpenApiTag {
  Graphql = "GraphQL",
  System = "System",
}

export enum OpenApiSecurityScheme {
  XClientId = "X-Client-ID",
}

export enum LogLevel {
  Debug,
  Verbose,
  Info,
  Http,
  Warn,
  Error,
}
