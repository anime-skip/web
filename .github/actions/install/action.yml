name: Install Dependencies
inputs:
  npmAuthToken:
    required: true
runs:
  using: composite
  steps:
    - run: pnpm install --immutable --immutable-cache
      shell: bash
      env:
        NODE_AUTH_TOKEN: ${{ inputs.npmAuthToken }}
